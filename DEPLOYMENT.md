# 🚀 Руководство по CI/CD и Удаленному Развертыванию

Это руководство описывает, как использовать настроенную систему CI/CD для удаленного обновления приложения на планшете без физического доступа к устройству.

## 📋 Содержание

1. [Как это работает](#как-это-работает)
2. [Первоначальная настройка](#первоначальная-настройка)
3. [Ежедневное использование](#ежедневное-использование)
4. [Создание релиза](#создание-релиза)
5. [Удаленная отладка](#удаленная-отладка)
6. [Решение проблем](#решение-проблем)

## 🎯 Как это работает

### Автоматизированный процесс

```
Вы push код → GitHub Actions собирает APK → APK доступен для скачивания → Установка на планшет
```

**Преимущества:**
- ✅ Не нужно физически забирать планшет с завода
- ✅ Автоматическая сборка при каждом изменении
- ✅ Удаленная установка через WiFi
- ✅ Удаленное чтение логов
- ✅ История всех версий в GitHub Releases

## 🔧 Первоначальная настройка

### 1. Настройка ADB через WiFi (Один раз)

**На планшете:**

```bash
# Подключите планшет к компьютеру через USB
adb devices

# Включите ADB через TCP/IP
adb tcpip 5555

# Узнайте IP адрес планшета
adb shell ip addr show wlan0 | grep "inet "
# Пример вывода: inet 192.168.1.100/24
```

**Запишите IP адрес планшета:** `_________________`

```bash
# Отключите USB кабель
# Подключитесь через WiFi
adb connect 192.168.1.100:5555

# Проверьте подключение
adb devices
# Должно показать: 192.168.1.100:5555  device
```

✅ **Готово!** Теперь можно работать с планшетом удаленно через WiFi.

### 2. Создание переменной окружения (Опционально)

Добавьте в `~/.bashrc` или `~/.zshrc`:

```bash
export TABLET_IP="192.168.1.100"
```

Затем:
```bash
source ~/.bashrc  # или source ~/.zshrc
```

Теперь можно использовать:
```bash
adb connect $TABLET_IP:5555
```

## 📱 Ежедневное использование

### Вариант A: Автоматический деплой (Рекомендуется)

**Шаг 1:** Сделайте изменения в коде

**Шаг 2:** Закоммитьте и запушьте:
```bash
git add .
git commit -m "fix: исправление бага с загрузкой задач"
git push origin main
```

**Шаг 3:** GitHub Actions автоматически соберет APK (≈ 5-10 минут)

**Шаг 4:** Скачайте и установите APK на планшет:
```bash
# Используйте скрипт deploy_to_device.sh
./deploy_to_device.sh 192.168.1.100
```

Или вручную:
```bash
# Скачать APK из последнего workflow
# Перейдите: https://github.com/shribusson/ksm_android_app/actions
# Скачайте artifact 'app-debug'

# Подключитесь к планшету
adb connect 192.168.1.100:5555

# Установите
adb install -r app-debug.apk

# Перезапустите приложение
adb shell am force-stop com.example.bitrix_app
adb shell am start -n com.example.bitrix_app/.MainActivity
```

### Вариант B: Использование скрипта деплоя

```bash
# Простая команда для обновления планшета
./deploy_to_device.sh 192.168.1.100
```

Скрипт автоматически:
1. Подключится к планшету
2. Скачает последний релиз
3. Установит APK
4. Перезапустит приложение
5. Покажет логи

## 🏷️ Создание релиза

Для стабильных версий создавайте релизы:

```bash
# Обновите версию в app/build.gradle.kts:
# versionCode = 2
# versionName = "1.1.0"

git add app/build.gradle.kts
git commit -m "chore: bump version to 1.1.0"
git push origin main

# Создайте тег
git tag v1.1.0
git push origin v1.1.0
```

GitHub Actions автоматически:
- Соберет Release APK
- Создаст GitHub Release
- Прикрепит APK к релизу

**Скачать релиз:**
```bash
wget https://github.com/shribusson/ksm_android_app/releases/download/v1.1.0/app-release.apk
adb install -r app-release.apk
```

## 🐛 Удаленная отладка

### Просмотр логов в реальном времени

```bash
# Подключитесь к планшету
adb connect 192.168.1.100:5555

# Смотрите логи Bitrix приложения
adb logcat | grep Bitrix

# Или все логи приложения
adb logcat --pid=$(adb shell pidof -s com.example.bitrix_app)

# Фильтр по уровню (Error, Warning)
adb logcat *:E | grep Bitrix
```

### Скачивание файлов логов

```bash
# Текущий лог файл
adb shell "run-as com.example.bitrix_app cat /data/data/com.example.bitrix_app/files/logs/app_log_current.txt" > app_log.txt

# Последний crash report
adb shell "run-as com.example.bitrix_app cat /data/data/com.example.bitrix_app/files/last_crash.txt" > crash_log.txt

# Открыть в редакторе
cat app_log.txt | less
```

### Удаленное выполнение команд

```bash
# Перезапуск приложения
adb shell am force-stop com.example.bitrix_app
adb shell am start -n com.example.bitrix_app/.MainActivity

# Очистка кэша приложения
adb shell pm clear com.example.bitrix_app

# Информация о приложении
adb shell dumpsys package com.example.bitrix_app | grep version

# Скриншот экрана планшета
adb exec-out screencap -p > screenshot.png
```

### Удаленная установка новой версии

```bash
# Быстрая переустановка с сохранением данных
adb install -r app-debug.apk

# Полная переустановка (удалит данные)
adb uninstall com.example.bitrix_app
adb install app-debug.apk
```

## 🔍 Решение проблем

### Проблема: "unable to connect to IP:5555"

**Причина:** ADB через WiFi отключился (перезагрузка планшета, таймаут)

**Решение:**
```bash
# Если есть физический доступ
adb usb  # переключить обратно на USB
adb tcpip 5555  # включить WiFi режим снова

# Или попросите кого-то на заводе выполнить:
# Настройки → О планшете → 7 раз нажать "Номер сборки"
# Настройки → Для разработчиков → Включить "Отладка по USB"
```

### Проблема: APK не устанавливается

**Ошибка:** `INSTALL_FAILED_UPDATE_INCOMPATIBLE`

**Решение:**
```bash
# Удалите старую версию
adb uninstall com.example.bitrix_app

# Установите заново
adb install app-debug.apk
```

### Проблема: Приложение не отображается в логах

**Решение:**
```bash
# Проверьте, запущено ли приложение
adb shell ps | grep bitrix_app

# Проверьте crashes
adb logcat -b crash

# Запустите приложение заново
adb shell am start -n com.example.bitrix_app/.MainActivity
```

### Проблема: GitHub Actions не запускается

**Проверьте:**
1. Actions включены в настройках репозитория
   - Settings → Actions → General → "Allow all actions"
2. Workflow файл синтаксически корректен
   - `.github/workflows/android-build.yml`
3. Push был в ветку `main` или `master`

## 📊 Мониторинг статуса сборки

### GitHub Actions

1. Откройте https://github.com/shribusson/ksm_android_app/actions
2. Посмотрите статус последнего workflow
3. Если ❌ (failed) - откройте лог для диагностики
4. Если ✅ (success) - APK готов для скачивания

### Badge в README

В README добавлен badge статуса сборки:

![Build Status](https://github.com/shribusson/ksm_android_app/actions/workflows/android-build.yml/badge.svg)

- 🟢 Green = сборка успешна
- 🔴 Red = сборка провалилась

## 📅 Рекомендованный workflow

### Ежедневная разработка

1. **Утром:** Проверьте логи планшета
   ```bash
   adb connect $TABLET_IP:5555
   adb shell "run-as com.example.bitrix_app cat /data/data/com.example.bitrix_app/files/logs/app_log_current.txt" | tail -100
   ```

2. **Разработка:** Вносите изменения локально

3. **Тестирование:** Соберите и протестируйте локально
   ```bash
   ./gradlew assembleDebug
   adb install -r app/build/outputs/apk/debug/app-debug.apk
   ```

4. **Push в main:** Когда всё работает
   ```bash
   git push origin main
   ```

5. **Деплой на планшет:** Через 5-10 минут после push
   ```bash
   ./deploy_to_device.sh $TABLET_IP
   ```

6. **Проверка:** Смотрите логи
   ```bash
   adb logcat | grep Bitrix
   ```

### Еженедельные релизы

1. Создайте тег для стабильной версии
2. GitHub Actions создаст релиз автоматически
3. Установите на планшет из релиза

## 🎓 Полезные команды

```bash
# Быстрое подключение
alias tablet="adb connect $TABLET_IP:5555"

# Быстрые логи
alias tablet-logs="adb logcat | grep Bitrix"

# Быстрая установка
alias tablet-install="adb install -r app/build/outputs/apk/debug/app-debug.apk"

# Быстрый перезапуск
alias tablet-restart="adb shell am force-stop com.example.bitrix_app && adb shell am start -n com.example.bitrix_app/.MainActivity"
```

Добавьте в `~/.bashrc` для постоянного использования.

## 📞 Контакты

При проблемах с CI/CD:
1. Проверьте Actions logs в GitHub
2. Проверьте README.md для инструкций
3. Проверьте этот файл (DEPLOYMENT.md) для troubleshooting

---

**Последнее обновление:** 2026-01-23
